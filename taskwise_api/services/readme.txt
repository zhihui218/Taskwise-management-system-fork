This "services" folder consists of all the functions under a specific object of a "collection" 
which is neccessary to do any operations and simple CRUD directly to database. It can also stores the functions
that are shared among the the whole "collection" rather than a single instance